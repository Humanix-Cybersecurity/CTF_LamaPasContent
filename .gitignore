# === RÉPONSES DU CTF — NE JAMAIS COMMITTER ===
# Vraies valeurs (flags + secrets) ; un ctf_secrets.example.json à placeholders
# est committé à la place.
app/ctf_secrets.json
# Solutions détaillées
writeups/
# Documents privés : writeup complet, guide de déploiement avec vrais secrets…
private/
# Script de régression : contient les exploits (= des réponses)
scripts/

# === Python ===
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# === Env / outils ===
.env
.env.*
.claude/
.DS_Store
